# GPU 加速 + 构建基础设施 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 MicroAgent 建立支持 CUDA/Vulkan/CPU 三种变体的构建基础设施，包括开发环境一键搭建脚本、参数化构建脚本，以及 GitHub Actions CI/CD 自动发布。

**Architecture:** 根目录只保留 `setup.bat`（环境搭建）和 `build.bat`（调度层），所有具体逻辑提取到 `buildscripts/` 目录中的专用脚本；GitHub Actions `release.yml` 复用同一套 buildscripts，三个变体并行构建，tag 触发自动发布到 GitHub Release。

**Tech Stack:** Windows cmd batch scripts, Nuitka (Python→exe), llama-cpp-python (CUDA pre-built wheel / Vulkan from-source / CPU PyPI), GitHub Actions, winget (Vulkan SDK 安装)

---

### Task 1: 拆分 requirements 文件

**Files:**
- Create: `requirements-base.txt`
- Modify: `requirements.txt`
- Modify: `requirements-dev.txt`

背景：当前 `requirements.txt` 混合了 llama-cpp-python（需按 GPU 变体区分安装方式）和其他通用依赖。拆分后，`buildscripts/install-deps.bat` 可以先安装正确的 llama-cpp-python 变体，再安装其余依赖。同时修正 `duckduckgo-search` → `ddgs`（已在 `tools/web_search.py` 中更新，requirements 需同步）。

- [ ] **Step 1: 创建 `requirements-base.txt`**

内容（不含 llama-cpp-python）：

```
smolagents>=0.3.0
rich>=13.7.0
psutil>=5.9.0
pydantic>=2.6.0
pyyaml>=6.0.1
ddgs>=6.2.0
tavily-python>=0.3.3
```

- [ ] **Step 2: 将 `requirements.txt` 替换为**

```
-r requirements-base.txt
llama-cpp-python>=0.2.90
```

- [ ] **Step 3: 将 `requirements-dev.txt` 替换为**

```
-r requirements-base.txt
pytest>=8.1.0
pytest-mock>=3.14.0
```

- [ ] **Step 4: 验证三个文件语法正确**

```cmd
pip install -r requirements-base.txt --dry-run
pip install -r requirements.txt --dry-run
pip install -r requirements-dev.txt --dry-run
```

Expected: 三条命令均无报错退出（exit code 0）。

- [ ] **Step 5: 确认现有测试仍通过**

```cmd
pytest tests/ -v
```

Expected: 所有测试 PASS。

- [ ] **Step 6: Commit**

```cmd
git add requirements-base.txt requirements.txt requirements-dev.txt
git commit -m "chore: split requirements into base + variant, fix duckduckgo-search -> ddgs"
```

---

### Task 2: buildscripts/detect-gpu.bat

**Files:**
- Create: `buildscripts/detect-gpu.bat`

检测当前机器 GPU 类型，设置 `DETECTED_GPU` 变量供调用方读取。**不使用 `setlocal`**，确保变量传播到调用方。

- [ ] **Step 1: 创建 `buildscripts/detect-gpu.bat`**

```bat
@echo off
:: buildscripts/detect-gpu.bat
:: Detects current GPU type and sets DETECTED_GPU=cuda|vulkan|cpu
:: Usage: call buildscripts\detect-gpu.bat
::        then read %DETECTED_GPU%
:: Note: No setlocal — DETECTED_GPU propagates to the caller's environment

nvidia-smi >nul 2>&1
if not errorlevel 1 (
    set DETECTED_GPU=cuda
    echo [detect-gpu] NVIDIA GPU detected ^(cuda^)
    exit /b 0
)

wmic path win32_VideoController get Caption 2>nul | findstr /i "AMD Radeon" >nul
if not errorlevel 1 (
    set DETECTED_GPU=vulkan
    echo [detect-gpu] AMD Radeon GPU detected ^(vulkan^)
    exit /b 0
)

set DETECTED_GPU=cpu
echo [detect-gpu] No dedicated GPU detected ^(cpu^)
exit /b 0
```

- [ ] **Step 2: 验证（在当前 NVIDIA 4080 构建机上）**

```cmd
call buildscripts\detect-gpu.bat
echo DETECTED_GPU=%DETECTED_GPU%
```

Expected:
```
[detect-gpu] NVIDIA GPU detected (cuda)
DETECTED_GPU=cuda
```

- [ ] **Step 3: Commit**

```cmd
git add buildscripts/detect-gpu.bat
git commit -m "feat: add GPU auto-detection script"
```

---

### Task 3: buildscripts/install-vulkan-sdk.bat

**Files:**
- Create: `buildscripts/install-vulkan-sdk.bat`

幂等安装 Vulkan SDK。在 GitHub Actions 中额外将 `VULKAN_SDK` 写入 `GITHUB_ENV`，使后续 steps 能读到该变量。

- [ ] **Step 1: 创建 `buildscripts/install-vulkan-sdk.bat`**

```bat
@echo off
:: buildscripts/install-vulkan-sdk.bat
:: Installs Vulkan SDK via winget if not already present.
:: Idempotent: skips if VULKAN_SDK env var already points to a valid SDK.
:: In GitHub Actions: writes VULKAN_SDK path to GITHUB_ENV for subsequent steps.
:: Usage: call buildscripts\install-vulkan-sdk.bat

setlocal EnableDelayedExpansion

:: Check if already installed (VULKAN_SDK env var + lib file present)
if defined VULKAN_SDK (
    if exist "%VULKAN_SDK%\Lib\vulkan-1.lib" (
        echo [install-vulkan-sdk] Already installed: %VULKAN_SDK%
        endlocal & set VULKAN_SDK=%VULKAN_SDK%
        exit /b 0
    )
)

echo [install-vulkan-sdk] Installing Vulkan SDK via winget...
winget install KhronosGroup.VulkanSDK --accept-source-agreements --accept-package-agreements
if errorlevel 1 (
    echo [install-vulkan-sdk] ERROR: winget install failed
    exit /b 1
)

:: Discover installed SDK path (newest version under C:\VulkanSDK\)
set SDK_PATH=
for /d %%D in ("C:\VulkanSDK\*") do set SDK_PATH=%%D

if "!SDK_PATH!"=="" (
    echo [install-vulkan-sdk] ERROR: Cannot find SDK under C:\VulkanSDK\
    exit /b 1
)

echo [install-vulkan-sdk] Installed: !SDK_PATH!

:: In GitHub Actions: export to GITHUB_ENV so subsequent steps see VULKAN_SDK
if defined GITHUB_ENV (
    echo VULKAN_SDK=!SDK_PATH!>> "%GITHUB_ENV%"
    echo [install-vulkan-sdk] VULKAN_SDK written to GITHUB_ENV
)

:: Propagate VULKAN_SDK out of setlocal scope to caller
endlocal & set VULKAN_SDK=%SDK_PATH%
exit /b 0
```

- [ ] **Step 2: 验证幂等性（SDK 已安装时应直接跳过）**

```cmd
call buildscripts\install-vulkan-sdk.bat
echo Exit: %errorlevel%
echo VULKAN_SDK=%VULKAN_SDK%
```

Expected（SDK 已安装时）:
```
[install-vulkan-sdk] Already installed: C:\VulkanSDK\1.x.x.x
Exit: 0
VULKAN_SDK=C:\VulkanSDK\1.x.x.x
```

- [ ] **Step 3: Commit**

```cmd
git add buildscripts/install-vulkan-sdk.bat
git commit -m "feat: add idempotent Vulkan SDK installation script"
```

---

### Task 4: buildscripts/install-deps.bat

**Files:**
- Create: `buildscripts/install-deps.bat`

按变体安装 llama-cpp-python，随后安装 `requirements-base.txt`。

**CUDA 变体：** 使用官方预编译 wheel（`cu124` 默认，兼容 CUDA 12.x 和 CUDA 13.x）。可通过 `LLAMA_CUDA_VERSION` 环境变量覆盖（如 `set LLAMA_CUDA_VERSION=cu125`）。

**Vulkan 变体：** 从源码编译，需要 `VULKAN_SDK`、VS Build Tools、CMake。

**CPU 变体：** 标准 PyPI wheel。

- [ ] **Step 1: 创建 `buildscripts/install-deps.bat`**

```bat
@echo off
:: buildscripts/install-deps.bat
:: Installs llama-cpp-python for the specified GPU variant, then requirements-base.txt
::
:: Usage: install-deps.bat <cuda|vulkan|cpu>
::
:: CUDA: Uses official pre-built wheel. No GPU or CUDA Toolkit needed at build time.
::   Default CUDA wheel: cu124 (compatible with CUDA 12.x and 13.x via NVIDIA backward compat)
::   Override: set LLAMA_CUDA_VERSION=cu125 before calling
::
:: Vulkan: Compiles llama-cpp-python from source. Prerequisites:
::   - VULKAN_SDK env var set (run install-vulkan-sdk.bat first)
::   - VS 2022 Build Tools with C++ workload
::   - CMake in PATH
::
:: CPU: Standard PyPI wheel, no GPU needed.

setlocal

set VARIANT=%1
if "%VARIANT%"=="" (
    echo [install-deps] ERROR: variant required ^(cuda^|vulkan^|cpu^)
    exit /b 1
)

echo [install-deps] Installing llama-cpp-python ^(%VARIANT%^)...

if /i "%VARIANT%"=="cuda" (
    if "%LLAMA_CUDA_VERSION%"=="" set LLAMA_CUDA_VERSION=cu124
    echo [install-deps] CUDA wheel: %LLAMA_CUDA_VERSION%
    pip install "llama-cpp-python>=0.2.90" ^
        --extra-index-url "https://abetlen.github.io/llama-cpp-python/whl/%LLAMA_CUDA_VERSION%"
    if errorlevel 1 (
        echo [install-deps] ERROR: CUDA wheel install failed
        exit /b 1
    )
    goto :install_base
)

if /i "%VARIANT%"=="vulkan" (
    if not defined VULKAN_SDK (
        echo [install-deps] ERROR: VULKAN_SDK not set. Run buildscripts\install-vulkan-sdk.bat first.
        exit /b 1
    )
    echo [install-deps] Building Vulkan wheel ^(VULKAN_SDK=%VULKAN_SDK%^)...
    set CMAKE_ARGS=-DGGML_VULKAN=on
    pip install "llama-cpp-python>=0.2.90" --no-binary :all:
    if errorlevel 1 (
        echo [install-deps] ERROR: Vulkan source build failed
        exit /b 1
    )
    goto :install_base
)

if /i "%VARIANT%"=="cpu" (
    pip install "llama-cpp-python>=0.2.90"
    if errorlevel 1 (
        echo [install-deps] ERROR: CPU wheel install failed
        exit /b 1
    )
    goto :install_base
)

echo [install-deps] ERROR: Unknown variant: %VARIANT%
exit /b 1

:install_base
echo [install-deps] Installing requirements-base.txt...
pip install -r requirements-base.txt
if errorlevel 1 (
    echo [install-deps] ERROR: requirements-base.txt install failed
    exit /b 1
)
echo [install-deps] Done.
endlocal
exit /b 0
```

- [ ] **Step 2: 验证 CPU 变体（最快，无需 GPU）**

在已激活 venv 的环境中：

```cmd
buildscripts\install-deps.bat cpu
echo Exit: %errorlevel%
```

Expected:
```
[install-deps] Installing llama-cpp-python (cpu)...
[install-deps] Installing requirements-base.txt...
[install-deps] Done.
Exit: 0
```

- [ ] **Step 3: Commit**

```cmd
git add buildscripts/install-deps.bat
git commit -m "feat: add variant-aware dependency installer (cuda/vulkan/cpu)"
```

---

### Task 5: buildscripts/build-variant.bat

**Files:**
- Create: `buildscripts/build-variant.bat`

从现有 `build.bat` 提取 Nuitka 编译逻辑，增加：
- 每个变体独立的 build/dist 目录：`build\nuitka\<variant>\` 和 `dist\<variant>\`
- 输出 exe 命名为 `microagent-<variant>.exe`

- [ ] **Step 1: 创建 `buildscripts/build-variant.bat`**

```bat
@echo off
:: buildscripts/build-variant.bat
:: Compiles MicroAgent for one GPU variant using Nuitka.
::
:: Usage: build-variant.bat <cuda|vulkan|cpu> [release]
::   cuda|vulkan|cpu : GPU backend variant
::   release         : --onefile mode (distributable single exe)
::   (no mode arg)   : --standalone mode (faster, for local dev/testing)
::
:: Output:
::   dev:     dist\<variant>\microagent-<variant>.exe  (+ sibling DLLs)
::   release: dist\<variant>\microagent-<variant>.exe  (single file)

setlocal

set VARIANT=%1
set MODE=%2
if "%VARIANT%"=="" (
    echo [build-variant] ERROR: variant required ^(cuda^|vulkan^|cpu^)
    exit /b 1
)
if "%MODE%"=="" set MODE=dev

set BUILD_DIR=build\nuitka\%VARIANT%
set DIST_DIR=dist\%VARIANT%
set EXE_NAME=microagent-%VARIANT%.exe

echo [build-variant] Variant=%VARIANT%  Mode=%MODE%
echo [build-variant] Output: %DIST_DIR%\%EXE_NAME%

:: Clean dist dir, preserve models\ (may contain large .gguf files)
if exist "%DIST_DIR%" (
    for /d %%D in ("%DIST_DIR%\*") do if /i not "%%~nxD"=="models" rmdir /s /q "%%D"
    del /q "%DIST_DIR%\*" 2>nul
)
if not exist "%DIST_DIR%" mkdir "%DIST_DIR%"
if not exist "%BUILD_DIR%" mkdir "%BUILD_DIR%"

:: Install Nuitka (+ release-only compression extras)
if /i "%MODE%"=="release" (
    pip install nuitka zstandard ordered-set
) else (
    pip install nuitka
)

:: Common Nuitka flags shared by both modes
set NUITKA_FLAGS=^
  --include-package=smolagents ^
  --include-package-data=smolagents ^
  --include-package=llama_cpp ^
  --include-package=rich ^
  --include-package=psutil ^
  --include-package=pydantic ^
  --include-package=yaml ^
  --include-package=ddgs ^
  --include-package=tavily ^
  --output-filename=%EXE_NAME% ^
  --output-dir="%BUILD_DIR%" ^
  --assume-yes-for-downloads ^
  --lto=no ^
  --nofollow-import-to=colorama.win32 ^
  --nofollow-import-to=starlette ^
  --nofollow-import-to=starlette_context

if /i "%MODE%"=="release" (
    python -m nuitka --onefile %NUITKA_FLAGS% main.py
) else (
    python -m nuitka --standalone %NUITKA_FLAGS% main.py
)
if errorlevel 1 (
    echo [build-variant] Build FAILED.
    exit /b 1
)

:: Stage distribution artifacts
if /i "%MODE%"=="release" (
    copy "%BUILD_DIR%\%EXE_NAME%" "%DIST_DIR%\%EXE_NAME%"
) else (
    xcopy /e /i /y "%BUILD_DIR%\main.dist" "%DIST_DIR%"
)
copy config.yaml "%DIST_DIR%\config.yaml"
copy README.txt "%DIST_DIR%\README.txt"
if not exist "%DIST_DIR%\models" mkdir "%DIST_DIR%\models"

echo.
echo [build-variant] Done: %DIST_DIR%\%EXE_NAME%
endlocal
exit /b 0
```

- [ ] **Step 2: 验证 cpu dev 构建（最快，约 5 min，有 clcache 命中则更快）**

```cmd
buildscripts\build-variant.bat cpu dev
```

Expected 最后几行:
```
[build-variant] Done: dist\cpu\microagent-cpu.exe
```

验证产物存在：
```cmd
dir dist\cpu\microagent-cpu.exe
```

Expected: 文件存在，大小 > 1 MB。

- [ ] **Step 3: 验证 exe 可启动（快速冒烟测试）**

```cmd
dist\cpu\microagent-cpu.exe --help 2>nul || dist\cpu\microagent-cpu.exe
```

如果 exe 启动并进入 CLI 提示符（`>`），输入 `exit` 退出。Expected: 无崩溃，正常退出。

- [ ] **Step 4: Commit**

```cmd
git add buildscripts/build-variant.bat
git commit -m "feat: add variant-aware Nuitka build script (cuda/vulkan/cpu, dev/release)"
```

---

### Task 6: 重构 build.bat

**Files:**
- Modify: `build.bat`

将现有 `build.bat`（内含完整 Nuitka 命令）替换为薄调度层，调用 `buildscripts\build-variant.bat`。

- [ ] **Step 1: 将 `build.bat` 完整替换为**

```bat
@echo off
:: build.bat
:: Local build entry point — thin dispatcher to buildscripts\build-variant.bat
::
:: Usage: build.bat [cuda|vulkan|cpu|all] [release]
::   cuda|vulkan|cpu  : build single variant (default: cuda)
::   all              : build all three variants sequentially
::   release          : onefile distributable (default: standalone dev mode)
::
:: Examples:
::   build.bat                  -> cuda, dev (fast)
::   build.bat vulkan           -> vulkan, dev
::   build.bat cuda release     -> cuda, onefile release
::   build.bat all release      -> all three release variants

setlocal

set VARIANT=%1
set MODE=%2
if "%VARIANT%"=="" set VARIANT=cuda
if "%MODE%"=="" set MODE=dev

:: Activate .venv if present and not already in a virtual env
if not defined VIRTUAL_ENV (
    if exist ".venv\Scripts\activate.bat" (
        call .venv\Scripts\activate.bat
    )
)

if /i "%VARIANT%"=="all" (
    echo [build] Building all variants ^(mode: %MODE%^)...
    call buildscripts\build-variant.bat cuda %MODE%
    if errorlevel 1 exit /b 1
    call buildscripts\build-variant.bat vulkan %MODE%
    if errorlevel 1 exit /b 1
    call buildscripts\build-variant.bat cpu %MODE%
    if errorlevel 1 exit /b 1
    echo.
    echo [build] All variants complete:
    echo   dist\cuda\microagent-cuda.exe
    echo   dist\vulkan\microagent-vulkan.exe
    echo   dist\cpu\microagent-cpu.exe
) else (
    call buildscripts\build-variant.bat %VARIANT% %MODE%
    if errorlevel 1 exit /b 1
)

endlocal
exit /b 0
```

- [ ] **Step 2: 验证 build.bat cpu dev（应调用 build-variant.bat）**

```cmd
build.bat cpu dev
```

Expected: 调用 `buildscripts\build-variant.bat cpu dev`，产出 `dist\cpu\microagent-cpu.exe`（如有 clcache 命中则很快完成）。

- [ ] **Step 3: Commit**

```cmd
git add build.bat
git commit -m "refactor: convert build.bat to thin dispatcher, support cuda/vulkan/cpu/all"
```

---

### Task 7: setup.bat

**Files:**
- Create: `setup.bat`

- [ ] **Step 1: 创建 `setup.bat`**

**注意：** `if "%VARIANT%"==""` 使用两条独立 `if` 语句（非 block），确保 `%DETECTED_GPU%` 在 `detect-gpu.bat` 运行之后展开（避免 cmd.exe block 内变量预展开问题）。

```bat
@echo off
:: setup.bat
:: Developer environment setup for MicroAgent.
:: Creates .venv, detects GPU (or use arg), installs correct llama-cpp-python variant.
::
:: Usage: setup.bat [cuda|vulkan|cpu]
::   (no args: auto-detects GPU via buildscripts\detect-gpu.bat)
::
:: After setup: activate venv with  .venv\Scripts\activate.bat
:: Or run directly:                 .venv\Scripts\python main.py

setlocal

set VARIANT=%1

echo [setup] MicroAgent Developer Setup
echo.

:: 1. Verify Python is available
python --version >nul 2>&1
if errorlevel 1 (
    echo [setup] ERROR: Python not found. Install Python 3.10+ and add to PATH.
    exit /b 1
)
for /f "tokens=2" %%v in ('python --version 2^>^&1') do echo [setup] Python %%v

:: 2. Create .venv if not present
if not exist ".venv" (
    echo [setup] Creating virtual environment...
    python -m venv .venv
    if errorlevel 1 (
        echo [setup] ERROR: Failed to create .venv
        exit /b 1
    )
)

:: 3. Activate .venv (pip commands below install into .venv)
call .venv\Scripts\activate.bat

:: 4. Auto-detect GPU variant if not specified
::    Two separate 'if' lines so %DETECTED_GPU% expands AFTER detect-gpu.bat sets it
if "%VARIANT%"=="" call buildscripts\detect-gpu.bat
if "%VARIANT%"=="" set VARIANT=%DETECTED_GPU%
echo [setup] GPU variant: %VARIANT%

:: 5. Install Vulkan SDK if needed (idempotent)
if /i "%VARIANT%"=="vulkan" (
    call buildscripts\install-vulkan-sdk.bat
    if errorlevel 1 (
        echo [setup] ERROR: Vulkan SDK installation failed
        exit /b 1
    )
)

:: 6. Install variant-specific llama-cpp-python + base requirements
call buildscripts\install-deps.bat %VARIANT%
if errorlevel 1 (
    echo [setup] ERROR: Dependency installation failed
    exit /b 1
)

:: 7. Install dev tools (pytest etc.)
pip install -r requirements-dev.txt
if errorlevel 1 (
    echo [setup] ERROR: Dev requirements install failed
    exit /b 1
)

:: 8. Done
echo.
echo [setup] Setup complete!
echo   GPU variant : %VARIANT%
echo   Virtual env : .venv\
echo   Activate    : .venv\Scripts\activate.bat
echo   Run app     : .venv\Scripts\python main.py
echo   Run tests   : .venv\Scripts\pytest tests/ -v
echo   Build       : build.bat %VARIANT% release
echo.

endlocal
exit /b 0
```

- [ ] **Step 2: 验证 setup.bat cpu（最快，不需要 GPU）**

先删除 `.venv` 以测试完整流程：

```cmd
rmdir /s /q .venv
setup.bat cpu
```

Expected 最后几行:
```
[setup] Setup complete!
  GPU variant : cpu
  Virtual env : .venv\
  ...
```

- [ ] **Step 3: 验证测试在新 venv 中通过**

```cmd
.venv\Scripts\pytest tests/ -v
```

Expected: 所有测试 PASS。

- [ ] **Step 4: Commit**

```cmd
git add setup.bat
git commit -m "feat: add developer environment setup script with GPU auto-detection"
```

---

### Task 8: .github/workflows/release.yml

**Files:**
- Create: `.github/workflows/release.yml`

三个 build job 并行；`create-release` job 仅在 tag push 时运行，汇总所有 artifact 发布到 GitHub Release。

Vulkan job 额外缓存 pip wheel 目录（含编译好的 Vulkan llama-cpp-python wheel），避免每次重新编译（首次约 10-15 min，命中缓存后 < 1 min）。

- [ ] **Step 1: 创建 `.github/workflows/` 目录（如不存在）**

```cmd
mkdir .github\workflows
```

- [ ] **Step 2: 创建 `.github/workflows/release.yml`**

```yaml
name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      variant:
        description: 'Build variant'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - cuda
          - vulkan
          - cpu

jobs:
  build-cuda:
    name: Build (CUDA)
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.variant == 'all' || inputs.variant == 'cuda' }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pip\Cache
          key: pip-cuda-${{ hashFiles('requirements-base.txt') }}
          restore-keys: pip-cuda-

      - name: Cache Nuitka
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\Nuitka\Nuitka\Cache
          key: nuitka-cuda-${{ hashFiles('main.py', 'core/**', 'tools/**', 'ui/**', 'cli/**') }}
          restore-keys: nuitka-cuda-

      - name: Install dependencies (CUDA)
        run: buildscripts\install-deps.bat cuda
        shell: cmd

      - name: Build release (CUDA)
        run: build.bat cuda release
        shell: cmd

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: microagent-cuda
          path: dist\cuda\microagent-cuda.exe

  build-vulkan:
    name: Build (Vulkan)
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.variant == 'all' || inputs.variant == 'vulkan' }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache pip + Vulkan wheel
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pip\Cache
          key: pip-vulkan-${{ hashFiles('requirements-base.txt') }}-v1
          restore-keys: pip-vulkan-

      - name: Cache Nuitka
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\Nuitka\Nuitka\Cache
          key: nuitka-vulkan-${{ hashFiles('main.py', 'core/**', 'tools/**', 'ui/**', 'cli/**') }}
          restore-keys: nuitka-vulkan-

      - name: Install Vulkan SDK
        run: buildscripts\install-vulkan-sdk.bat
        shell: cmd

      - name: Install dependencies (Vulkan)
        run: buildscripts\install-deps.bat vulkan
        shell: cmd

      - name: Build release (Vulkan)
        run: build.bat vulkan release
        shell: cmd

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: microagent-vulkan
          path: dist\vulkan\microagent-vulkan.exe

  build-cpu:
    name: Build (CPU)
    if: ${{ github.event_name != 'workflow_dispatch' || inputs.variant == 'all' || inputs.variant == 'cpu' }}
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\pip\Cache
          key: pip-cpu-${{ hashFiles('requirements-base.txt') }}
          restore-keys: pip-cpu-

      - name: Cache Nuitka
        uses: actions/cache@v4
        with:
          path: ~\AppData\Local\Nuitka\Nuitka\Cache
          key: nuitka-cpu-${{ hashFiles('main.py', 'core/**', 'tools/**', 'ui/**', 'cli/**') }}
          restore-keys: nuitka-cpu-

      - name: Install dependencies (CPU)
        run: buildscripts\install-deps.bat cpu
        shell: cmd

      - name: Build release (CPU)
        run: build.bat cpu release
        shell: cmd

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: microagent-cpu
          path: dist\cpu\microagent-cpu.exe

  create-release:
    name: Create GitHub Release
    needs: [build-cuda, build-vulkan, build-cpu]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            artifacts/microagent-cuda/microagent-cuda.exe
            artifacts/microagent-vulkan/microagent-vulkan.exe
            artifacts/microagent-cpu/microagent-cpu.exe
            config.yaml
            README.txt
          generate_release_notes: true
```

- [ ] **Step 3: 验证 YAML 语法**

```cmd
python -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml').read()); print('YAML OK')"
```

Expected:
```
YAML OK
```

- [ ] **Step 4: Commit**

```cmd
git add .github/workflows/release.yml
git commit -m "ci: add GitHub Actions workflow for multi-variant release builds"
```

---

### Task 9: DEVELOPMENT.md

**Files:**
- Create: `DEVELOPMENT.md`

- [ ] **Step 1: 创建 `DEVELOPMENT.md`**

````markdown
# MicroAgent 开发文档

## 前提条件（一次性安装）

| 工具 | 最低版本 | 用途 |
|---|---|---|
| Python | 3.10+ | 运行时与构建 |
| Git | 任意 | 版本控制 |
| VS 2022 Build Tools | 17.x（含 C++ 工作负载） | Vulkan 变体编译 llama-cpp-python |
| CMake | 3.20+ | Vulkan 变体构建系统 |
| winget | 内置于 Windows 11 | 自动安装 Vulkan SDK |

下载链接：
- VS 2022 Build Tools：https://visualstudio.microsoft.com/downloads/#build-tools-for-visual-studio-2022
- CMake：https://cmake.org/download/（勾选 "Add CMake to system PATH"）

> **注：** CUDA 变体无需安装 CUDA Toolkit——使用官方预编译 wheel，任何 Windows 机器均可安装。

---

## 快速开始

```cmd
git clone <repo-url>
cd MicroAgent

:: 自动检测 GPU，创建 .venv，安装依赖
setup.bat

:: 放置模型文件（.gguf 格式）
copy path\to\your-model.gguf models\

:: 激活 venv 并运行
.venv\Scripts\activate.bat
python main.py
```

指定 GPU 变体（跳过自动检测）：

```cmd
setup.bat cuda      :: NVIDIA GPU（4070 TS、4080 等）
setup.bat vulkan    :: AMD Radeon（含 iGPU 780M 等）
setup.bat cpu       :: 无 GPU
```

---

## 本地构建 Release

```cmd
:: 单个变体 dev 模式（standalone，快速，用于本地测试）
build.bat cuda
build.bat vulkan
build.bat cpu

:: 单个变体 release（onefile，可分发）
build.bat cuda release
build.bat vulkan release
build.bat cpu release

:: 全部三种 release（顺序构建，约 30-45 min 首次，clcache 命中后更快）
build.bat all release
```

产物位置：`dist\<variant>\microagent-<variant>.exe`

---

## 触发 CI 构建（GitHub Actions）

推送版本 tag，自动并行构建三种变体并发布到 GitHub Release：

```cmd
git tag v0.2.0
git push origin v0.2.0
```

在 GitHub Actions 页面手动触发（用于测试单个变体的 CI 配置）：
Actions → Build and Release → Run workflow → 选择 variant。

---

## 项目结构

```
MicroAgent/
├── main.py                     # 入口：加载配置 → 组装 Agent → 启动 CLI
├── setup.bat                   # 开发环境搭建
├── build.bat                   # 构建入口（调度层）
├── buildscripts/
│   ├── detect-gpu.bat          # GPU 类型检测 (→ DETECTED_GPU=cuda|vulkan|cpu)
│   ├── install-vulkan-sdk.bat  # Vulkan SDK 安装（幂等）
│   ├── install-deps.bat        # 按变体安装 llama-cpp-python + 基础依赖
│   └── build-variant.bat       # Nuitka 编译逻辑
├── .github/workflows/
│   └── release.yml             # CI/CD：tag 触发，三变体并行构建
├── core/
│   ├── agent.py                # AgentRunner + ToolCallingAgent 实现
│   ├── model.py                # LlamaCppBackend + smolagents 适配
│   └── config.py               # pydantic 配置模型
├── tools/
│   ├── base.py                 # MicroTool 基类
│   ├── think.py                # ThinkTool（推理步骤展示）
│   ├── web_search.py           # DuckDuckGo / Tavily 搜索
│   ├── file_manager.py         # 文件系统操作
│   └── system_info.py          # CPU / 内存 / 电池状态
├── cli/app.py                  # CLI 交互主循环
├── ui/
│   ├── console.py              # 共享 Rich Console（pinned 到 terminal stdout）
│   └── logger.py               # 文件日志（stdout/stderr → log file）
├── requirements-base.txt       # 运行时依赖（不含 llama-cpp-python）
├── requirements.txt            # 向后兼容：base + llama-cpp-python CPU
└── requirements-dev.txt        # 开发依赖：base + pytest
```

---

## GPU 加速说明

| 变体 | 目标硬件 | llama-cpp-python | 运行时依赖 |
|---|---|---|---|
| cuda | NVIDIA GPU（4070 TS、4080 等） | 预编译 wheel cu124（兼容 CUDA 12.x/13.x） | NVIDIA 驱动 |
| vulkan | AMD Radeon（含 iGPU 780M）、Intel Arc、NVIDIA（备用） | 从源码编译（需 Vulkan SDK） | AMD/Intel/NVIDIA 驱动（含 Vulkan 支持） |
| cpu | 无 GPU | 标准 PyPI wheel | 无 |

**CUDA 版本兼容性：** cu124 wheel 兼容 CUDA 12.4+ 及 13.x（NVIDIA 运行时向后兼容保证）。
使用更新 wheel：

```cmd
set LLAMA_CUDA_VERSION=cu125
buildscripts\install-deps.bat cuda
```

**`config.yaml` 中 `n_gpu_layers` 说明：**
- `-1`：所有层卸载到 GPU（推荐，GPU 内存充足时）
- `0`：纯 CPU 推理（即使安装了 GPU 变体也不用 GPU）
- `N`：前 N 层卸载到 GPU（GPU 内存不足时分层）

---

## 新增工具指南

1. 在 `tools/` 下新建 `my_tool.py`：

```python
from tools.base import MicroTool

class MyTool(MicroTool):
    name = "my_tool"
    description = "工具描述（LLM 根据此描述决定何时调用）"
    inputs = {
        "param": {"type": "string", "description": "参数说明"},
    }
    output_type = "string"

    def forward(self, param: str) -> str:
        return f"结果: {param}"
```

2. 在 `core/config.py` 的 `ToolsConfig` 中添加配置项（参照 `SystemInfoConfig`）。

3. 在 `tools/registry.py` 中注册（参照现有工具）。

4. 在 `tests/tools/` 下添加测试文件（参照 `tests/tools/test_system_info.py`）。
````

- [ ] **Step 2: Commit**

```cmd
git add DEVELOPMENT.md
git commit -m "docs: add DEVELOPMENT.md with setup, build, CI, and GPU guide"
```

---

## 自审

**Spec 覆盖检查：**
- ✅ 环境搭建自动化 → Task 7 (setup.bat)
- ✅ GPU 自动检测 → Task 2 (detect-gpu.bat)
- ✅ CUDA 变体（NVIDIA 4070 TS） → Task 4 (install-deps.bat cuda), Task 5 (build-variant)
- ✅ Vulkan 变体（AMD Radeon 780M iGPU） → Task 3 (install-vulkan-sdk), Task 4 (vulkan), Task 5
- ✅ CPU 变体 → Task 4 (cpu), Task 5
- ✅ buildscripts/ 目录结构 → Tasks 2-5
- ✅ build.bat 重构为调度层 → Task 6
- ✅ setup.bat → Task 7
- ✅ GitHub Actions → Task 8
- ✅ requirements 拆分 → Task 1
- ✅ DEVELOPMENT.md → Task 9
- ✅ Vulkan SDK 幂等安装 → Task 3
- ✅ pip + Nuitka 缓存策略 → Task 8
- ✅ 发布产物（3 exe + config.yaml + README.txt） → Task 8 create-release job
- ✅ CUDA 13.1 兼容（cu124 backward compat + LLAMA_CUDA_VERSION 覆盖） → Task 4 注释

**变量/命名一致性：**
- `VARIANT` 值：`cuda` / `vulkan` / `cpu`（全小写，所有脚本一致）
- `MODE` 值：`dev` / `release`（所有脚本一致）
- exe 命名：`microagent-<variant>.exe`（一致）
- dist 目录：`dist\<variant>\`（一致）
- build 目录：`build\nuitka\<variant>\`（一致）

**无 Placeholder。**
